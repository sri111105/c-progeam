#include<bits/stdc++.h>
using namespace std;
int main()
{
     int n;
    cin >> n;
    string ans="";
    while(n>0)
        {
            int rem=n%2;
            ans+=(rem+'0');
            n=n/2;
        }
    reverse(ans.begin(),ans.end());
    cout<<ans;
    return 0;
}
