#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin >> n; 
    set<int> st;
    for (int i = 0; i < n; i++) {
        int val;
        cin >> val;
        st.insert(val); 
    }
    int first = INT_MIN;
    int second = INT_MIN;
    for (int x : st) {
        if (x > first) {
            second = first; 
            first = x;
        } else if (x > second && x != first) {
            second = x;
        }
    }
    if (second == INT_MIN) {
        cout << "No second largest element exists." << endl;
    } else {
        cout << second << endl;
    }
    return 0;
}
